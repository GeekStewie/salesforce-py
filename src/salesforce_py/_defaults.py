"""Project-level defaults for salesforce-py.

Values here are used as fallbacks across all sub-modules.
Override per-call by passing explicit arguments to the relevant class or function.
"""

DEFAULT_API_VERSION = "66.0"
